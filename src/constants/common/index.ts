export enum ExternalMessageEnum {
  MESSAGE = 'MESSAGE',
}

export enum ExternalLiProfile {
  LI_PROFILE = 'LINKEDIN_PROFILE',
}

export enum ExternalLiConnection {
  LI_CONNECTION = 'LINKED_CONNECTION',
}

export enum ExternalLiTotalConnection {
  LI_TOTAL_CONNECTION = 'LINKED_TOTAL_CONNECTION',
}

export enum ExternalLiSearchProfile {
  LI_GLOBAL_SEARCH = 'SEARCH_PROFILE',
}
